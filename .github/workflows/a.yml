name: continous-build
on: 
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
     - main
  workflow_dispatch:
# Testing our first workflow
env: 
  name: Gagandeep
jobs:
  build: 
    runs-on: ubuntu-latest
    env:
      environment: dev
      lastname: singh
    steps:
      - name: step1
        run: |
          echo "hello from $name $latname, job1 of workflow1 running in environment $environment"
          sleep 30
          hostname
      - name: my step2
        run: |
          echo "hello from $name"
          sleep 10
      - run: |
          echo "hello from step3"
          sleep 10
      - name: my step4 for testing multiple lines of code execution
        run: |
          ls -l
          pwd
          sudo cat /etc/os-release
          uname -a
          echo "final step4 execution done"
  test: 
    runs-on: gagan
    env:
      environment: staging
      lastname: singh
    steps:
      - name: step1
        run: |
           echo "hello from $name $lastname, job 2 of workflow1 running in environment $environment"
           sleep 30
           hostname
      - name: my step2
        run: |
          echo "hello from $name"  
          sleep 10

